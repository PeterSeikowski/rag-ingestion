[pytest]
pythonpath = src
testpaths = tests
markers =
    integration: requires real Redis + Elasticsearch running (see tests/integration/README.md)
    docling_integration: requires the real docling package installed
